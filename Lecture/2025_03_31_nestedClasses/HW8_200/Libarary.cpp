// Libarary.cpp
#include "Library.h"

Library::Library()
{
    // Creates a new instance of Library
    //  by setting _name to an empty string,
    _name = "";
    // _current_books to 0, 
    _cur_books = 0;
    // each entry in _page_counts to 0.
    _page_counts = 0;
}